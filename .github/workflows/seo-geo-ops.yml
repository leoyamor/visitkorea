  name: SEO GEO Daily Ops

  'on':
    workflow_dispatch:
    schedule:
      - cron: '17 2 * * *'

  jobs:
    test:
      runs-on: ubuntu-latest
      steps:
        - run: echo "workflow syntax OK"
